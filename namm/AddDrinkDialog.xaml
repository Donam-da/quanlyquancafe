<Window x:Class="namm.AddDrinkDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Thêm món" Height="180" Width="300"
        WindowStartupLocation="CenterOwner"
        WindowStyle="ToolWindow"
        Loaded="Window_Loaded">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="tbDrinkName" Grid.Row="0" Text="Tên đồ uống" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <TextBlock Text="Số lượng:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox x:Name="txtQuantity" Width="100" VerticalContentAlignment="Center" Text="1" KeyDown="TxtQuantity_KeyDown"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button x:Name="btnOk" Content="Đồng ý" Width="80" Margin="0,0,10,0" IsDefault="True" Click="BtnOk_Click"/>
            <Button x:Name="btnCancel" Content="Hủy" Width="80" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>