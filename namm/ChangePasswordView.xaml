<UserControl x:Class="namm.ChangePasswordView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:namm"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="400">
            <TextBlock Text="ĐỔI MẬT KHẨU" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,30"/>

            <TextBlock Text="Tên đăng nhập:" Margin="0,10,0,2"/>
            <TextBox x:Name="txtUserName" Padding="5" IsReadOnly="True" Background="#EEEEEE"/>

            <TextBlock Text="Mật khẩu cũ:" Margin="0,10,0,2"/>
            <PasswordBox x:Name="pwbOldPassword" Padding="5"/>

            <TextBlock Text="Mật khẩu mới:" Margin="0,10,0,2"/>
            <PasswordBox x:Name="pwbNewPassword" Padding="5"/>

            <TextBlock Text="Nhập lại mật khẩu mới:" Margin="0,10,0,2"/>
            <PasswordBox x:Name="pwbConfirmPassword" Padding="5"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,30,0,0">
                <Button x:Name="btnUpdate" Content="Cập nhật" Width="100" Margin="5" Click="BtnUpdate_Click"/>
                <Button x:Name="btnReset" Content="Làm mới" Width="100" Margin="5" Click="BtnReset_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>