<UserControl x:Class="namm.MaterialView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:namm"
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="1000"
             Loaded="UserControl_Loaded">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="350"/>
        </Grid.ColumnDefinitions>

        <!-- Cột 1: Danh sách nguyên liệu -->
        <DockPanel Grid.Column="0" Margin="0,0,10,0">
            <StackPanel DockPanel.Dock="Top" Orientation="Vertical">
                <TextBlock Text="DANH SÁCH NGUYÊN LIỆU" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                <TextBox x:Name="txtSearch" Margin="0,0,0,5" Height="25" ToolTip="Nhập tên nguyên liệu để tìm kiếm" TextChanged="TxtSearch_TextChanged"/>
            </StackPanel>
            <DataGrid x:Name="dgMaterials" AutoGenerateColumns="False" IsReadOnly="True"                      
                      SelectionMode="Single" SelectionChanged="DgMaterials_SelectionChanged" 
                      LoadingRow="DgMaterials_LoadingRow">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="STT" Binding="{Binding STT}" Width="50" />
                    <DataGridTextColumn Header="Tên nguyên liệu" Binding="{Binding Name}" Width="*" />
                    <DataGridTextColumn Header="Số lượng tồn" Binding="{Binding Quantity, StringFormat=G29}" Width="100" />
                    <DataGridTextColumn Header="Đơn vị" Binding="{Binding UnitName}" Width="80" />
                    <DataGridTextColumn Header="Đơn giá" Binding="{Binding Price, StringFormat={}{0:N0}}" Width="100" />
                    <DataGridTextColumn Header="Trạng thái" Binding="{Binding StatusText}" Width="100" />
                </DataGrid.Columns>
            </DataGrid>
        </DockPanel>

        <!-- Cột 2: Thông tin chi tiết và chức năng -->
        <DockPanel Grid.Column="1">
            <TextBlock DockPanel.Dock="Top" Text="THÔNG TIN CHI TIẾT" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
            <StackPanel>
                <TextBlock Text="Tên nguyên liệu:" Margin="0,5,0,2"/>
                <TextBox x:Name="txtName" Padding="5"/>

                <Grid Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Margin="0,0,5,0">
                        <TextBlock Text="Số lượng tồn:" Margin="0,0,0,2"/>
                        <TextBox x:Name="txtQuantity" Padding="5"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Margin="5,0,0,0">
                        <TextBlock Text="Đơn giá:" Margin="0,0,0,2"/>
                        <TextBox x:Name="txtPrice" Padding="5"/>
                    </StackPanel>
                </Grid>

                <TextBlock Text="Đơn vị tính:" Margin="0,10,0,2"/>
                <ComboBox x:Name="cbUnit" Padding="5" DisplayMemberPath="Name" SelectedValuePath="ID"/>

                <TextBlock Text="Ghi chú:" Margin="0,10,0,2"/>
                <TextBox x:Name="txtDescription" Padding="5" Height="60" TextWrapping="Wrap"/>

                <CheckBox x:Name="chkIsActive" Content="Đang sử dụng" Margin="0,10,0,0" IsChecked="True"/>

                <WrapPanel HorizontalAlignment="Center" Margin="0,20,0,0">
                    <Button x:Name="btnAdd" Content="Thêm" Width="80" Margin="5" Click="BtnAdd_Click"/>
                    <Button x:Name="btnEdit" Content="Sửa" Width="80" Margin="5" Click="BtnEdit_Click"/>
                    <Button x:Name="btnDelete" Content="Xóa" Width="80" Margin="5" Click="BtnDelete_Click"/>
                </WrapPanel>
                <Button x:Name="btnReset" Content="Làm mới" Width="80" Margin="5" HorizontalAlignment="Center" Click="BtnReset_Click"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</UserControl>