<UserControl x:Class="namm.LoyalCustomerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:namm"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000"
             FontFamily="Segoe UI" Loaded="UserControl_Loaded">
    <DockPanel Margin="10">
        <!-- Tiêu đề -->
        <Border DockPanel.Dock="Top" Padding="5" Margin="0,0,0,10" BorderBrush="LightGray" BorderThickness="0,0,0,1">
            <TextBlock Text="THỐNG KÊ KHÁCH HÀNG THÂN THIẾT" 
                       FontSize="20" 
                       FontWeight="Bold" 
                       Foreground="#2E4053" 
                       HorizontalAlignment="Center"/>
        </Border>

        <!-- Khu vực thiết lập giảm giá -->
        <GroupBox DockPanel.Dock="Top" Header="Thiết lập các mức giảm giá" FontWeight="SemiBold" Margin="0,0,0,10">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Form thêm quy tắc mới -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBlock Text="Tiêu chí:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <ComboBox x:Name="cbCriteriaType" Width="120" VerticalContentAlignment="Center">
                        <ComboBoxItem Content="Số lần mua"/>
                        <ComboBoxItem Content="Tổng chi tiêu"/>
                    </ComboBox>
                    <TextBlock Text="Ngưỡng >" VerticalAlignment="Center" Margin="15,0,5,0"/>
                    <TextBox x:Name="txtThreshold" Width="120" VerticalContentAlignment="Center"/>
                    <TextBlock Text="Giảm giá (%):" VerticalAlignment="Center" Margin="15,0,5,0"/>
                    <TextBox x:Name="txtDiscountPercent" Width="80" VerticalContentAlignment="Center"/>
                    <Button x:Name="btnAddRule" Content="Thêm mức" Width="100" Margin="15,0,0,0" Click="BtnAddRule_Click"/>
                </StackPanel>

                <!-- Danh sách các quy tắc đã thêm -->
                <ListView Grid.Row="1" x:Name="lvDiscountRules" MaxHeight="150" HorizontalAlignment="Left" SelectionChanged="LvDiscountRules_SelectionChanged">
                    <ListView.Resources>
                        <Style TargetType="GridViewColumnHeader">
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        </Style>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        </Style>
                    </ListView.Resources>
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Width="50">
                                <GridViewColumn.Header>
                                    <!-- Checkbox ở header để chọn/bỏ chọn tất cả -->
                                    <CheckBox Click="RuleHeaderCheckBox_Click" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </GridViewColumn.Header>
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox IsChecked="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListViewItem}}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Tiêu chí" Width="150">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding CriteriaType}" HorizontalAlignment="Center"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Ngưỡng" Width="150">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Threshold, StringFormat={}{0:N0}}" HorizontalAlignment="Center"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Giảm giá (%)" Width="100">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding DiscountPercent, StringFormat={}{0:G29}}" HorizontalAlignment="Center"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>

                <!-- Nút điều khiển -->
                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <Button x:Name="btnApplyRuleToAll" Content="Áp dụng cho tất cả KH" Margin="0,0,10,0" Click="BtnApplyRuleToAll_Click" IsEnabled="{Binding ElementName=lvDiscountRules, Path=SelectedItems.Count, Converter={StaticResource NullToFalseConverter}}" Background="#28A745" Foreground="White"/>
                    <Button x:Name="btnRemoveRule" Content="Xóa mức đã chọn" Margin="0,0,10,0" Click="BtnRemoveRule_Click" IsEnabled="False" Background="#DC3545" Foreground="White"/>
                </StackPanel>
            </Grid>
        </GroupBox>


        <!-- Bảng dữ liệu chính và Popup chi tiết -->
        <Grid DockPanel.Dock="Bottom">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>


            <Grid.Resources>
                <!-- Style để căn giữa nội dung trong các ô -->
                <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
                <Style x:Key="CenterCellStyle" TargetType="TextBlock">
                    <Setter Property="HorizontalAlignment" Value="Center" />
                </Style>
            </Grid.Resources>

            <!-- Popup để hiển thị chi tiết các mức giảm giá -->
            <Popup x:Name="popupAppliedRules"
                   StaysOpen="False"/>
            <!-- Bảng dữ liệu khách hàng -->
            <DataGrid Grid.Row="0" x:Name="dgLoyalCustomers" 
                      AutoGenerateColumns="False" 
                      IsReadOnly="True"
                      CanUserSortColumns="True"
                      SelectionMode="Extended"
                      GridLinesVisibility="Horizontal"
                      AlternatingRowBackground="#F5F5F5"
                      HorizontalContentAlignment="Center"
                      LoadingRow="DgLoyalCustomers_LoadingRow">
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <EventSetter Event="PreviewMouseLeftButtonDown" Handler="DataGridRow_PreviewMouseLeftButtonDown_Selection"/>
                    </Style>
                </DataGrid.RowStyle>
                <DataGrid.Resources>
                    <!-- Căn giữa cho tiêu đề cột -->
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    </Style>
                </DataGrid.Resources>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="STT" Binding="{Binding STT}" Width="0.5*" ElementStyle="{StaticResource CenterCellStyle}"/>
                    <DataGridTextColumn Header="Tên khách hàng" Binding="{Binding CustomerName}" Width="2*" ElementStyle="{StaticResource CenterCellStyle}"/>
                    <DataGridTextColumn Header="Mã Khách hàng" Binding="{Binding CustomerCode}" Width="1.5*" ElementStyle="{StaticResource CenterCellStyle}"/>
                    <DataGridTextColumn Header="Số ĐT" Binding="{Binding PhoneNumber}" Width="1.2*" ElementStyle="{StaticResource CenterCellStyle}"/>
                    <DataGridTextColumn Header="Địa chỉ" Binding="{Binding Address}" Width="2.5*" ElementStyle="{StaticResource CenterCellStyle}"/>
                    <DataGridTextColumn Header="Số lần mua" Binding="{Binding PurchaseCount}" Width="1*" ElementStyle="{StaticResource CenterCellStyle}"/>
                    <DataGridTextColumn Header="Tổng chi tiêu" Binding="{Binding TotalSpent, StringFormat={}{0:N0} VNĐ}" Width="1.5*" SortDirection="Descending" ElementStyle="{StaticResource CenterCellStyle}"/>
                    <DataGridTextColumn Header="Mức giảm giá" Binding="{Binding AppliedRuleDescription}" Width="1.5*" ElementStyle="{StaticResource CenterCellStyle}"/>
                    <DataGridTextColumn Header="Tiền được giảm" Binding="{Binding Discount, StringFormat={}{0:N0} VNĐ}" Width="1*" ElementStyle="{StaticResource CenterCellStyle}"/>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Nút Xóa Khách hàng -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button x:Name="btnDeleteCustomer" Content="Xóa khách hàng đã chọn" Click="BtnDeleteCustomer_Click" 
                        IsEnabled="{Binding ElementName=dgLoyalCustomers, Path=SelectedItems.Count, Converter={StaticResource NullToFalseConverter}}" Background="#DC3545" Foreground="White"/>
            </StackPanel>
        </Grid>
    </DockPanel>
</UserControl>