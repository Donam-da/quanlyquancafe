<UserControl x:Class="namm.ProfitStatisticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:namm"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000"
             FontFamily="Segoe UI" Loaded="UserControl_Loaded">
    <DockPanel Margin="10">
        <!-- Tiêu đề và bộ lọc -->
        <StackPanel DockPanel.Dock="Top">
            <TextBlock Text="THỐNG KÊ LỢI NHUẬN" 
                       FontSize="20" 
                       FontWeight="Bold" 
                       Foreground="#2E4053" 
                       HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,10"/>

            <Border Padding="10" Margin="0,0,0,10" BorderBrush="LightGray" BorderThickness="0,0,0,1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="Từ ngày:" VerticalAlignment="Center" Margin="5"/>
                    <DatePicker Grid.Column="1" x:Name="dpStartDate" Width="120" VerticalContentAlignment="Center" SelectedDateChanged="DpStartDate_SelectedDateChanged"/>
                    <TextBlock Grid.Column="2" Text="Đến ngày:" VerticalAlignment="Center" Margin="15,5,5,5"/>
                    <DatePicker Grid.Column="3" x:Name="dpEndDate" Width="120" VerticalContentAlignment="Center"/>
                    <Button Grid.Column="4" x:Name="btnFilter" Content="Lọc" Width="80" Height="30" Margin="15,0" Click="BtnFilter_Click"/>

                    <!-- Phần tóm tắt -->
                    <StackPanel Grid.Column="5" Orientation="Horizontal" HorizontalAlignment="Right">
                        <StackPanel Margin="10,0">
                            <TextBlock Text="Tổng Doanh thu" HorizontalAlignment="Center" Foreground="Gray"/>
                            <TextBlock x:Name="tbTotalRevenue" Text="0 VNĐ" HorizontalAlignment="Center" FontWeight="Bold" FontSize="16" Foreground="DarkBlue"/>
                        </StackPanel>
                        <TextBlock Text="-" VerticalAlignment="Center" FontSize="20" Margin="5,10,5,0"/>
                        <StackPanel Margin="10,0">
                            <TextBlock Text="Tổng Vốn" HorizontalAlignment="Center" Foreground="Gray"/>
                            <TextBlock x:Name="tbTotalCost" Text="0 VNĐ" HorizontalAlignment="Center" FontWeight="Bold" FontSize="16" Foreground="DarkRed"/>
                        </StackPanel>
                        <TextBlock Text="=" VerticalAlignment="Center" FontSize="20" Margin="5,10,5,0"/>
                        <StackPanel Margin="10,0">
                            <TextBlock Text="Tổng Lợi nhuận" HorizontalAlignment="Center" Foreground="Gray"/>
                            <TextBlock x:Name="tbTotalProfit" Text="0 VNĐ" HorizontalAlignment="Center" FontWeight="Bold" FontSize="18" Foreground="DarkGreen"/>
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </Border>
        </StackPanel>

        <!-- Bộ lọc cột -->
        <Border DockPanel.Dock="Top" Padding="5" Margin="0,0,0,5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Tên đồ uống:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <ComboBox Grid.Column="1" x:Name="cbFilterDrinkName" SelectionChanged="Filters_Changed" DisplayMemberPath="Name" SelectedValuePath="ID"/>

                <TextBlock Grid.Column="2" Text="Kiểu:" VerticalAlignment="Center" Margin="10,0,5,0"/>
                <ComboBox Grid.Column="3" x:Name="cbFilterDrinkType" SelectionChanged="Filters_Changed"/>

                <TextBlock Grid.Column="4" Text="Loại:" VerticalAlignment="Center" Margin="10,0,5,0"/>
                <ComboBox Grid.Column="5" x:Name="cbFilterCategory" SelectionChanged="Filters_Changed" DisplayMemberPath="Name" SelectedValuePath="ID"/>

            </Grid>
        </Border>

        <!-- Bảng dữ liệu -->
        <DataGrid x:Name="dgProfitStats" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True"
                  CanUserSortColumns="True"
                  GridLinesVisibility="Horizontal"
                  AlternatingRowBackground="#F5F5F5">
            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                </Style>
                <Style x:Key="CenterCellStyle" TargetType="TextBlock">
                    <Setter Property="HorizontalAlignment" Value="Center" />
                </Style>
                <Style x:Key="RightCellStyle" TargetType="TextBlock">
                    <Setter Property="HorizontalAlignment" Value="Right" />
                    <Setter Property="Margin" Value="0,0,5,0"/>
                </Style>
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTextColumn Header="STT" Binding="{Binding STT}" Width="0.5*" ElementStyle="{StaticResource CenterCellStyle}"/>
                <DataGridTextColumn Header="Tên đồ uống" Binding="{Binding DrinkName}" Width="2.5*" ElementStyle="{StaticResource CenterCellStyle}"/>
                <DataGridTextColumn Header="Kiểu" Binding="{Binding DrinkType}" Width="1*" ElementStyle="{StaticResource CenterCellStyle}"/>
                <DataGridTextColumn Header="SL Bán" Binding="{Binding TotalQuantitySold}" Width="0.8*" ElementStyle="{StaticResource CenterCellStyle}"/>
                <DataGridTextColumn Header="Giá vốn (đơn vị)" Binding="{Binding UnitCost, StringFormat={}{0:N0}}" Width="1.2*" ElementStyle="{StaticResource CenterCellStyle}"/>
                <DataGridTextColumn Header="Tổng Vốn" Binding="{Binding TotalCost, StringFormat={}{0:N0} VNĐ}" Width="1.2*" ElementStyle="{StaticResource CenterCellStyle}"/>
                <DataGridTextColumn Header="Doanh thu" Binding="{Binding TotalRevenue, StringFormat={}{0:N0} VNĐ}" Width="1.2*" ElementStyle="{StaticResource CenterCellStyle}"/>
                <DataGridTextColumn Header="Lợi nhuận" Binding="{Binding Profit, StringFormat={}{0:N0} VNĐ}" Width="1.2*" SortDirection="Descending" ElementStyle="{StaticResource CenterCellStyle}">
                    <DataGridTextColumn.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Foreground" Value="DarkGreen"/>
                        </Style>
                    </DataGridTextColumn.CellStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Tỷ suất (%)" Binding="{Binding ProfitMargin, StringFormat={}{0:N1}%}" Width="1*" ElementStyle="{StaticResource CenterCellStyle}">
                    <DataGridTextColumn.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Foreground" Value="DarkGreen"/>
                        </Style>
                    </DataGridTextColumn.CellStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>