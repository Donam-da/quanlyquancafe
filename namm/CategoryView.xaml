<UserControl x:Class="namm.CategoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:namm"
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="1000"
             Loaded="UserControl_Loaded">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="350"/>
        </Grid.ColumnDefinitions>

        <!-- Cột 1: Danh sách loại đồ uống -->
        <DockPanel Grid.Column="0" Margin="0,0,10,0">
            <StackPanel DockPanel.Dock="Top" Orientation="Vertical">
                <TextBlock Text="DANH SÁCH LOẠI ĐỒ UỐNG" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                <TextBox x:Name="txtSearch" Margin="0,0,0,5" Height="25" ToolTip="Nhập tên loại đồ uống để tìm kiếm" TextChanged="TxtSearch_TextChanged"/>
            </StackPanel>
            <DataGrid x:Name="dgCategories" AutoGenerateColumns="False" IsReadOnly="True"                      
                      SelectionMode="Single" SelectionChanged="DgCategories_SelectionChanged" 
                      LoadingRow="DgCategories_LoadingRow">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="STT" Binding="{Binding STT}" Width="50" />
                    <DataGridTextColumn Header="Tên loại" Binding="{Binding Name}" Width="*" />
                    <DataGridTextColumn Header="Trạng thái" Binding="{Binding StatusText}" Width="100" />
                </DataGrid.Columns>
            </DataGrid>
        </DockPanel>

        <!-- Cột 2: Thông tin chi tiết và chức năng -->
        <DockPanel Grid.Column="1">
            <TextBlock DockPanel.Dock="Top" Text="THÔNG TIN CHI TIẾT" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
            <StackPanel>
                <TextBlock Text="Tên loại đồ uống:" Margin="0,5,0,2"/>
                <TextBox x:Name="txtName" Padding="5"/>

                <CheckBox x:Name="chkIsActive" Content="Sử dụng" IsChecked="True" VerticalAlignment="Center" Margin="0,15,0,10"/>

                <WrapPanel HorizontalAlignment="Center" Margin="0,20,0,0">
                    <Button x:Name="btnAdd" Content="Thêm" Width="80" Margin="5" Click="BtnAdd_Click"/>
                    <Button x:Name="btnEdit" Content="Sửa" Width="80" Margin="5" Click="BtnEdit_Click"/>
                    <Button x:Name="btnDelete" Content="Xóa" Width="80" Margin="5" Click="BtnDelete_Click" Background="LightCoral"/>
                </WrapPanel>
                <Button x:Name="btnReset" Content="Làm mới" Width="80" Margin="5" HorizontalAlignment="Center" Click="BtnReset_Click"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</UserControl>